#include <iostream>
using namespace std;
int main() {

    // BITWISE OPERATORS

    cout<<(3 & 5) << endl;
    cout<<(3 | 5) << endl;
    cout<<(3 ^ 5) << endl;
    

    // BINARY NOT  OPERATOR

    cout << (~6) << endl;

    return 0;
}
